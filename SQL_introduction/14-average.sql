-- File: 14-average.sql
-- Description: This script computes the average score of all records in the table second_table
-- in the specified database.

-- Compute the average score
SELECT AVG(score) AS average FROM second_table;
