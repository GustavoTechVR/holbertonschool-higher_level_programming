-- File: 4-first_table.sql
-- Description: This script creates the table first_table in the specified database.

-- Create the table if it doesn't exist
CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
);
